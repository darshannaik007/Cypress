{
  "name": "CypressDemo",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "pretest": "rimraf ./cypress/results/json/*.json",
    "test": "cypress run || generate-mochawesome-report",
    "posttest": "generate-mochawesome-report"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cypress": "^6.6.0",
    "cypress-downloadfile": "^1.2.0",
    "rimraf": "^3.0.2",
    "typescript": "^4.2.2"
  },
  "devDependencies": {
    "@4tw/cypress-drag-drop": "^1.6.0",
    "cypress-file-upload": "^5.0.2",
    "cypress-mochawesome-reporter": "^1.3.0"
  }
}
